package ai.careerpilot.workflowplanner;

/** Phase 8 — a coarse, deterministic complexity estimate derived from step count. See {@link WorkflowEstimator}. */
public enum WorkflowComplexity {
    LOW, MEDIUM, HIGH
}
