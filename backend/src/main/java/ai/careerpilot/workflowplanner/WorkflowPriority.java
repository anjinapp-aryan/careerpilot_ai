package ai.careerpilot.workflowplanner;

/** Phase 8 — caller-supplied urgency for a {@link WorkflowPlanRequest}; does not affect plan validity, only estimation. */
public enum WorkflowPriority {
    LOW, MEDIUM, HIGH, CRITICAL
}
